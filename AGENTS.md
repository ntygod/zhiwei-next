# AGENTS.md

> 本文件是仓库级最小工作入口。知微由 AI 作为主要开发者持续推进；不要把全部知识复制到这里，按任务和目录逐层读取。

## 规则如何生效

- 本文件覆盖整个仓库。
- 进入含有更深层 `AGENTS.md` 的目录时，叠加该目录的局部规则。
- 局部规则可以收紧要求，但不得静默放松本文件的全仓红线。
- 系统、开发者和用户的明确指令优先；长期决策变化必须同步 ADR 或治理记录。
- 详细原理放在 `docs/`；`AGENTS.md` 只保留行动规则、导航和验收入口。
- 当前任务以任务开始时读取到的规则为准；本任务修改的规则从下一次独立任务开始生效。

## 自主开发授权

项目所有者授予 AI 对本仓库的长期开发权限。除平台或系统安全限制外，AI 可以自主：

- 选择和拆分下一项工作；
- 创建、更新和关闭 Issue；
- 创建分支、提交、推送、创建和更新 PR；
- 修改代码、文档、测试、依赖、工作流、仓库治理文件和 `AGENTS.md`；
- 在自动化门禁满足后审查并合并 PR；
- 创建迁移、执行重构、修复 CI、发布版本和维护项目状态。

默认不等待人工批准。人类评论、验收结果或明确指令一旦出现，应作为最高优先级的项目输入处理；没有人类反馈不构成阻塞。

自主权不等于降低标准：正常变更必须通过分支和 PR 留下历史；不得用直接写 `main`、隐藏风险、削弱测试或修改规则来逃避验证。完整授权与合并协议见 `docs/harness/autonomy-policy.md`。

## 先定位，再读取

开始工作前：

1. 确认任务触及的路径、当前里程碑和用户可感知结果。
2. 阅读目标路径最近的 `AGENTS.md`，不要预先加载整个仓库的规则。
3. 根据下表只阅读完成任务所需的来源。
4. 写清一句话目标、明确不做、风险等级和验证方式；范围扩大时重新检查里程碑。

| 任务类型 | 必读来源 |
|---|---|
| 自主开发流程、工作选择、PR 或规则维护 | `docs/harness/README.md`、`docs/harness/AGENTS.md` |
| 产品行为、交互或 UI | `docs/product/`、`docs/AGENTS.md` |
| 架构、模块边界或依赖方向 | `docs/architecture/system-architecture.md`、相关 `docs/adr/`、`packages/AGENTS.md` |
| 领域对象、记忆、作用域或生命周期 | `docs/architecture/domain-model.md`、目标包的局部规则 |
| Pi Runtime、事件或 Worker | `docs/architecture/pi-integration.md`、当前里程碑、`packages/pi-adapter/AGENTS.md` |
| 持久化、迁移或回放 | 当前里程碑、`packages/memory-store/AGENTS.md` |
| 安全、隐私、权限或外部副作用 | `docs/architecture/trust-and-safety.md`、`SECURITY.md` |
| 路线图、Issue 或里程碑拆分 | `docs/planning/roadmap.md`、当前里程碑、`docs/planning/acceptance-criteria.md` |

不要因为“可能相关”而通读全部文档。任务进入新的边界时，再加载该边界的规则。

## 来源与冲突处理

- 已接受 ADR：长期技术决策的事实源。
- 当前里程碑：当前范围、顺序和明确不做的事实源。
- 架构与领域文档：目标结构和语义的事实源。
- 代码与测试：当前已经实现的事实源。
- `docs/harness/project-state.md`：当前进展、阻塞和下一步候选的连续性快照。
- UI 原型：交流意图的样例，不是业务或架构事实源。

来源发生冲突时，不选择最方便的一份。可逆冲突由 AI 采用最小风险方案并同步事实源；涉及长期架构、数据删除、安全边界或外部副作用时，新增或 supersede ADR，并按风险模型完成独立 AI 审查后继续。

## 北极星

知微是本地优先的个人认知 Agent。优先级依次为：

1. 记得对；
2. 改得掉；
3. 说得清；
4. 不过界；
5. 最后才是功能数量。

## 全仓红线

1. 不自研通用 Agent Loop；Pi 是默认 Runtime，但不是认知真源。
2. Pi SDK 类型只能出现在 `packages/pi-adapter`。
3. Observation 是不可变证据；没有证据不能形成可使用的 MemoryClaim。
4. 任何读取先做作用域过滤，再做相关性排序；跨 Workspace 泄漏目标为零。
5. 用户明确纠正必须创建新版本并 supersede 旧版本，不能原地覆盖历史。
6. 派生索引、缓存和模型生成物都不是真源，必须可从结构化数据重建。
7. Attention 只提出值得关注的事项，默认不能直接产生外部副作用。
8. 不展示或持久化模型原始思维链；只保留行动、依据、结果和必要摘要。
9. 不为旧知微保留兼容层，不以“旧版有这个功能”作为实现理由。
10. 不提交密钥、真实个人记忆、模型原始转储、本地数据库或可识别用户的数据。
11. 不借当前任务顺手实现未来里程碑；当前范围以规划文档为准。
12. 不通过修改治理规则、跳过检查、删除测试、放宽断言或静默 fallback 让当前变更通过。

## 自主工作协议

### 选择工作

- GitHub Issue 和当前里程碑是工作队列；没有合适 Issue 时，AI 可以先创建再领取。
- 优先级：安全与数据完整性 > 主分支回归 > 当前里程碑阻塞 > 用户可感知纵向切片 > 维护性改进。
- 同一 Agent 同时只推进一个主要目标。发现邻近问题时记录 Issue，不扩大当前 PR。
- 详细选择算法和阻塞处理见 `docs/harness/development-loop.md`。

### 开工

在 Issue、PR 描述或工作记录中明确：

- 用户可感知的结果；
- 修改范围；
- 明确不做；
- 风险等级 `R0`–`R3`；
- 验证方式和回滚路径。

### 实现

- 选择最小的端到端切片，不做横向铺开的空壳框架。
- 输入在边界校验；核心逻辑保持确定性。
- 时间、ID、随机数、模型调用和 I/O 从边界注入。
- 不使用静默 fallback 掩盖能力缺失、数据损坏或协议漂移。
- 不修改无关代码；发现邻近问题时记录，而不是顺手扩大提交。
- 先证明行为，再抽象复用；不要为假设中的未来 Provider、数据库或 UI 提前设计。

### 审查与合并

- `R0/R1`：作者 AI 完成自审和全部自动化检查后可自主合并。
- `R2/R3`：必须由新的独立 AI 上下文审查当前 HEAD，并在 PR 留下可机器识别的批准记录。
- PR 只有在非 Draft、CI 成功、风险声明真实、回滚要求满足且不存在阻塞评论时才可合并。
- 默认使用 squash merge；PR 历史是项目所有者的主要审计界面。
- 自动合并协议见 `docs/harness/risk-model.md` 和 `.github/workflows/autonomous-merge.yml`。

### 交付

- 行为变化必须有不变量测试或用户场景测试。
- 架构决策变化必须新增或 supersede ADR，不能只改代码或叙述性文档。
- 文档只更新真正的事实源，避免在多个文件复制同一规则。
- 更新受影响的 Issue、当前项目状态和已知限制。
- 汇报已验证内容、未验证内容、风险、回滚和后续项。

## AGENTS 与 Harness 自维护

AI 负责持续维护本仓库的规则与 Harness：

- 路径、命令和事实变化应在同一 PR 中机械同步。
- 新增、删除或改变规则语义时，选择能够完整覆盖问题的最深目录。
- 治理变更按至少 `R2` 处理；涉及自动合并、安全、隐私或质量门时按 `R3` 处理。
- 治理 PR 不得依赖它自己新增或放宽的规则获得通过；旧规则约束整个当前任务。
- `R2/R3` 治理变更需要独立 AI 审查当前 HEAD 后才能合并。
- 一条规则只保留一个事实源；父级只做导航，不复制完整正文。
- 只有当目录拥有至少三条稳定、局部且无法由父级表达的规则时，才新增更深层 `AGENTS.md`。

详细维护协议见 `docs/harness/self-maintenance.md`。

## 变更触发的附加义务

| 变更 | 必须同时处理 |
|---|---|
| 领域状态、生命周期或作用域语义 | 不变量测试；必要时更新 `domain-model.md` |
| 包依赖、进程职责或技术选型 | 架构检查；新增或 supersede ADR |
| Runtime 协议、事件含义或关联键 | Contract Fixture、兼容性说明、版本决策 |
| 数据库 Schema 或迁移 | 前向迁移、回滚/恢复说明、真实存储测试；不得改写已应用迁移 |
| 隐私、权限或外部副作用 | 默认拒绝/询问策略、滥用场景、审计与脱敏验证 |
| 用户界面 | 正常、空、加载、错误、权限、降级状态及可访问性 |
| 当前里程碑范围 | 同步里程碑文档与验收标准，不只改路线图标题 |
| AGENTS、Harness、CI 或自动合并 | 风险至少 R2；同步治理文档、机器检查和独立审查要求 |

## 验证

提交或交付前默认运行：

```bash
npm run check
```

还要执行局部 `AGENTS.md` 指定的检查。不能通过删除测试、跳过架构检查、放宽断言或隐藏错误来获得绿色结果。若环境阻止某项验证，必须在 PR 明确未运行的命令、原因、风险和替代证据。

## Git 与发布

- 开发流程、提交格式和 PR 要求见 `CONTRIBUTING.md`。
- 一个分支和 PR 只解决一个可验证目标。
- 使用 Conventional Commits；避免 `misc`、`update` 等无信息提交。
- 正常工作不得直接写 `main`；紧急恢复也应随后补充 PR 或事故记录。
- AI 可以自主创建、更新、审查和合并 PR，也可以维护版本和发布流程。
- 不重写共享历史，不覆盖他人未合并的工作。

## 自主决策与阻塞

不要因为缺少人工响应而停滞：

- 可逆且局部的歧义：选择最简单、最安全、最符合当前里程碑的方案并记录理由。
- 长期架构或协议选择：写 ADR，完成独立 AI 审查后实施。
- 缺少凭证、外部权限或不可获得的环境：记录阻塞 Issue，保存可验证的部分成果，然后选择下一项不受阻工作。
- 破坏性迁移、删除语义或安全边界变化：必须提供恢复方案、验证恢复路径，并按 `R3` 合并。
- 人类明确要求暂停、改变方向或验收失败：立即停止相关自动推进，更新项目状态并按新指令调整。

只有在物理上无法继续、存在相互矛盾且不可逆的明确指令，或缺少必须由所有者提供的外部凭证时，才等待人工输入。

## 局部规则索引

- Harness 治理：`docs/harness/AGENTS.md`
- 文档：`docs/AGENTS.md`
- 应用与进程：`apps/AGENTS.md`
- 包通用边界：`packages/AGENTS.md`
- 纯领域：`packages/domain/AGENTS.md`
- 认知状态转换：`packages/cognition-core/AGENTS.md`
- 上下文编译：`packages/context-compiler/AGENTS.md`
- Runtime 中立协议：`packages/protocol/AGENTS.md`
- Pi 边界：`packages/pi-adapter/AGENTS.md`
- 持久化：`packages/memory-store/AGENTS.md`
