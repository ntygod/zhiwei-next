# AGENTS.md

> 本文件是仓库级的最小工作入口。不要把全部产品、架构和编码知识复制到这里；按任务和目录逐层读取。

## 规则如何生效

- 本文件覆盖整个仓库。
- 进入含有更深层 `AGENTS.md` 的目录时，叠加该目录的局部规则。
- 局部规则可以收紧要求，但不得放松本文件的全仓红线。
- 系统、开发者和用户的明确指令优先；若它们要求突破已接受 ADR 或全仓红线，不得静默执行，必须说明影响并同步决策记录。
- 详细原理放在 `docs/`；`AGENTS.md` 只保留行动规则、导航和验收入口。

## 先定位，再读取

开始工作前：

1. 确认任务触及的路径、当前里程碑和用户可感知结果。
2. 阅读目标路径最近的 `AGENTS.md`；不要预先加载整个仓库的规则。
3. 根据下表只阅读完成任务所需的来源。
4. 写清一句话目标、明确不做、验证方式；范围扩大时重新检查里程碑。

| 任务类型 | 必读来源 |
|---|---|
| 产品行为、交互或 UI | `docs/product/`、`docs/AGENTS.md` |
| 架构、模块边界或依赖方向 | `docs/architecture/system-architecture.md`、相关 `docs/adr/`、`packages/AGENTS.md` |
| 领域对象、记忆、作用域或生命周期 | `docs/architecture/domain-model.md`、目标包的局部规则 |
| Pi Runtime、事件或 Worker | `docs/architecture/pi-integration.md`、当前里程碑、`packages/pi-adapter/AGENTS.md` |
| 持久化、迁移或回放 | 当前里程碑、`packages/memory-store/AGENTS.md` |
| 安全、隐私、权限或外部副作用 | `docs/architecture/trust-and-safety.md`、`SECURITY.md` |
| 路线图、Issue 或里程碑拆分 | `docs/planning/roadmap.md`、当前里程碑、`docs/planning/acceptance-criteria.md` |

不要因为“可能相关”而通读全部文档。任务进入新的边界时，再加载该边界的规则。

## 来源与冲突处理

- 已接受 ADR：长期技术决策的事实源。
- 当前里程碑：当前范围、顺序和明确不做的事实源。
- 架构与领域文档：目标结构和语义的事实源。
- 代码与测试：当前已经实现的事实源。
- UI 原型：交流意图的样例，不是业务或架构事实源。

这些来源发生实质冲突时，不要自行选择最方便的一份。先指出冲突，再通过修正文档、补充 ADR 或缩小任务范围解决。

## 北极星

知微是本地优先的个人认知 Agent。优先级依次为：

1. 记得对；
2. 改得掉；
3. 说得清；
4. 不过界；
5. 最后才是功能数量。

## 全仓红线

1. 不自研通用 Agent Loop；Pi 是默认 Runtime，但不是认知真源。
2. Pi SDK 类型只能出现在 `packages/pi-adapter`。
3. Observation 是不可变证据；没有证据不能形成可使用的 MemoryClaim。
4. 任何读取先做作用域过滤，再做相关性排序；跨 Workspace 泄漏目标为零。
5. 用户明确纠正必须创建新版本并 supersede 旧版本，不能原地覆盖历史。
6. 派生索引、缓存和模型生成物都不是真源，必须可从结构化数据重建。
7. Attention 只提出值得关注的事项，默认不能直接产生外部副作用。
8. 不展示或持久化模型原始思维链；只保留行动、依据、结果和必要摘要。
9. 不为旧知微保留兼容层，不以“旧版有这个功能”作为实现理由。
10. 不提交密钥、真实个人记忆、模型原始转储、本地数据库或可识别用户的数据。
11. 不借当前任务顺手实现未来里程碑；当前范围以规划文档为准。

## 工作协议

### 开工

在 Issue、PR 描述或工作记录中明确：

- 用户可感知的结果；
- 修改范围；
- 明确不做；
- 主要风险；
- 验证方式。

### 实现

- 选择最小的端到端切片，不做横向铺开的空壳框架。
- 输入在边界校验；核心逻辑保持确定性。
- 时间、ID、随机数、模型调用和 I/O 从边界注入。
- 不使用静默 fallback 掩盖能力缺失、数据损坏或协议漂移。
- 不修改无关代码；发现邻近问题时记录，而不是顺手扩大提交。
- 先证明行为，再抽象复用；不要为假设中的未来 Provider、数据库或 UI 提前设计。

### 交付

- 行为变化必须有不变量测试或用户场景测试。
- 架构决策变化必须新增或 supersede ADR，不能只改代码或叙述性文档。
- 文档只更新真正的事实源，避免在多个文件复制同一规则。
- 汇报已验证内容、未验证内容、已知限制和后续项。

## 变更触发的附加义务

| 变更 | 必须同时处理 |
|---|---|
| 领域状态、生命周期或作用域语义 | 不变量测试；必要时更新 `domain-model.md` |
| 包依赖、进程职责或技术选型 | 架构检查；新增或 supersede ADR |
| Runtime 协议、事件含义或关联键 | Contract Fixture、兼容性说明、版本决策 |
| 数据库 Schema 或迁移 | 前向迁移、回滚/恢复说明、真实存储测试；不得改写已应用迁移 |
| 隐私、权限或外部副作用 | 默认拒绝/询问策略、滥用场景、审计与脱敏验证 |
| 用户界面 | 正常、空、加载、错误、权限、降级状态及可访问性 |
| 当前里程碑范围 | 同步里程碑文档与验收标准，不只改路线图标题 |

## 验证

提交或交付前默认运行：

```bash
npm run check
```

还要执行局部 `AGENTS.md` 指定的检查。不能通过删除测试、跳过架构检查、放宽断言或隐藏错误来获得绿色结果。若环境阻止某项验证，必须明确说明未运行的命令、原因和风险。

## Git 与发布

- 开发流程、提交格式和 PR 要求见 `CONTRIBUTING.md`。
- 一个分支和 PR 只解决一个可验证目标。
- 使用 Conventional Commits；避免“misc”“update”等无信息提交。
- 未经明确授权，不创建或合并 PR、不发布版本、不改仓库设置。
- 不重写共享历史，不覆盖他人未合并的工作。

## 不要猜测的情况

遇到以下情况，应先澄清或记录决策，再继续实现：

- 需求跨越当前里程碑的明确非目标；
- ADR、架构文档、代码行为相互冲突；
- 需要破坏性数据迁移或改变删除语义；
- 新增远程数据发送、凭证访问或自动外部行动；
- 修改协议必填字段、事件顺序、幂等或因果语义；
- 引入会长期塑造仓库的新框架、数据库或大型依赖。

## 局部规则索引

- 文档：`docs/AGENTS.md`
- 应用与进程：`apps/AGENTS.md`
- 包通用边界：`packages/AGENTS.md`
- 纯领域：`packages/domain/AGENTS.md`
- 认知状态转换：`packages/cognition-core/AGENTS.md`
- 上下文编译：`packages/context-compiler/AGENTS.md`
- Runtime 中立协议：`packages/protocol/AGENTS.md`
- Pi 边界：`packages/pi-adapter/AGENTS.md`
- 持久化：`packages/memory-store/AGENTS.md`

只有当某个目录拥有至少三条稳定、局部且无法由父级表达的规则时，才新增更深层 `AGENTS.md`。