# ADR 0002：本地优先、单用户

- 状态：Accepted
- 日期：2026-08-11

## 决策

知微的核心产品保持单用户、本地优先。SQLite 和本地 Artifact Store 是默认真源；云端同步只能是可选能力。

## 原因

长期记忆、权限和行为历史具有高敏感度。多租户、账户体系和服务端运维会显著扩大产品边界，并削弱用户数据所有权。

## 后果

默认不实现组织、多租户和服务端控制平面。未来跨设备同步必须端到端加密，且本地离线仍可工作。
